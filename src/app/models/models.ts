export interface ApiData {
  data: string[];
}

export interface CatFact {
  fact: string;
  image: string;
}

export interface CatImage {
  id: number;
  height: number;
  width: number;
  url: string;
}
